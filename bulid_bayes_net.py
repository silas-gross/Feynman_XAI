#This will take the data and build the Bayes net, as well as take the renormalization and build the Bayes net from that
#Also generates the visual output
#This particular file is the one that is most restricted in scope
#Others are more broadly applicable to later stages, but this is intended to just give validation of process
import Renormalization from Renormalization_scheme
import matplotlib.pyplot as mp
#import data

#then build bayes net 
def build_net_from_data:

#build net from renormalization proceducre
def build_net_from_renom:
    r=Renormalization()

